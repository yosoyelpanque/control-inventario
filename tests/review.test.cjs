const test=require('node:test'),assert=require('node:assert/strict'),{review}=require('../src/review.js');
const state={resguardantes:[{id:'u',name:'Ana',area:'A'}],inventory:[{'CLAVE UNICA':'100001',DESCRIPCION:'Uno',SERIE:' ABC ',areaOriginal:'A',UBICADO:'NO'},{'CLAVE UNICA':'100002',DESCRIPCION:'Dos',SERIE:'abc',areaOriginal:'B',UBICADO:'SI'},{'CLAVE UNICA':'100003',DESCRIPCION:'Tres',SERIE:'S/N',areaOriginal:'A',UBICADO:'NO'},{'CLAVE UNICA':'100004',DESCRIPCION:'Cuatro',SERIE:'S/N',areaOriginal:'A',UBICADO:'NO'}],additionalItems:[{id:'x',descripcion:'Personal',personal:'Si',resguardanteId:'u',usuario:'Ana',ubicacionEspecifica:'Oficina',tieneFormatoEntrada:false}]};
test('revisión ignora series repetidas y separa pendientes de errores',()=>{const before=JSON.stringify(state),r=review(state,'A');assert.equal(r.total,4);assert.equal(r.pending.length,3);assert.equal(r.issues.length,1);assert.deepEqual(r.issues[0].types,['entry']);assert.equal(JSON.stringify(state),before);});
test('usuario huérfano queda visible sin área y los ubicados requieren usuario y ubicación',()=>{const r=review({...state,additionalItems:[{id:'x',descripcion:'Bien',claveAsignada:'ARR-001',resguardanteId:'missing',usuario:'Ana'}]});assert.ok(r.issues.find(x=>x.id==='100002').reasons.includes('Sin ubicación'));assert.equal(r.issues.find(x=>x.id==='x').area,'Sin área asignada');});
